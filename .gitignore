# Credentials / secrets
.env
.env.*
!.env.example
*.pem
*.key
*credentials*
*secret*

# Markdown — ignore all docs/notes; keep only the public README
*.md
!README.md

# Personal / local-only state
state.json
instructions.txt

# Claude Code local config
.claude/
CLAUDE.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.swp

# Runtime data (snapshots, logs, pids, local caches)
data/snapshots/
data/kalshi_snapshots/
data/polymarket_snapshots/
data/*.log
data/*.pid
data/stdout.log
data/dashboard.log
data/_baseline_*
data/_refactor_*
data/match_report.json
data/match_report_props.json
data/paper_trades.jsonl
data/paper_settlements.jsonl
data/paper_closes.jsonl
data/sanity_rejected.jsonl
data/real_trades.jsonl
data/real_fills.jsonl
data/real_settlements.jsonl
data/real_closes.jsonl
data/real_halt.json
data/balance_snapshots.jsonl

# Probe artifacts (NOTES.md inside each is also ignored by *.md rule above)
data/kalshi_probe/
data/pinnacle_probe/
data/polymarket_probe/
